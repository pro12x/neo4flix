╔══════════════════════════════════════════════════════════════════╗
║     GUIDE COMPLET - Résolution Authentification 500             ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────┐
│ 🎯 PROBLÈME                                                      │
└──────────────────────────────────────────────────────────────────┘

  POST /api/v1/auth/login → HTTP 500 Internal Server Error

  CAUSE: Services backend PAS démarrés

┌──────────────────────────────────────────────────────────────────┐
│ ✅ SOLUTION - Démarrer les services (3 options)                 │
└──────────────────────────────────────────────────────────────────┘

╔═══════════════════════════════════════════════════════════════╗
║  OPTION 1: Docker Compose (LE PLUS SIMPLE) ⭐⭐⭐              ║
╚═══════════════════════════════════════════════════════════════╝

  cd /home/pro12x/Desktop/Projects/01Dakar/Java/neo4flix
  docker-compose up -d

  Avantages:
  ✓ Démarre tout automatiquement
  ✓ Pas besoin de terminaux multiples
  ✓ Services redémarrent automatiquement si crash

  Vérification:
  docker-compose ps
  curl http://localhost:1111/actuator/health

╔═══════════════════════════════════════════════════════════════╗
║  OPTION 2: Script start-all.sh (Terminaux séparés)           ║
╚═══════════════════════════════════════════════════════════════╝

  cd /home/pro12x/Desktop/Projects/01Dakar/Java/neo4flix
  ./start-all.sh

  Avantages:
  ✓ Chaque service dans son propre terminal
  ✓ Voir les logs en direct
  ✓ Facile de redémarrer un seul service

╔═══════════════════════════════════════════════════════════════╗
║  OPTION 3: Manuel (Pour debug) - 5 terminaux                 ║
╚═══════════════════════════════════════════════════════════════╝

  Terminal 1 - Neo4j:
  ───────────────────
  docker-compose up neo4j

  Terminal 2 - Eureka:
  ────────────────────
  cd eureka-server
  mvn spring-boot:run

  Terminal 3 - User Service:
  ──────────────────────────
  cd user-service
  mvn spring-boot:run

  Terminal 4 - API Gateway:
  ─────────────────────────
  cd api-gateway
  mvn spring-boot:run

  Terminal 5 - Movie Service:
  ───────────────────────────
  cd movie-service
  mvn spring-boot:run

┌──────────────────────────────────────────────────────────────────┐
│ 🧪 VÉRIFICATION (après 1-2 min)                                 │
└──────────────────────────────────────────────────────────────────┘

  # 1. Services UP?
  curl http://localhost:1111/actuator/health
  curl http://localhost:8081/actuator/health

  # 2. Login fonctionne?
  curl -X POST http://localhost:1111/api/v1/auth/login \
    -H "Content-Type: application/json" \
    -d '{"email":"john@example.com","password":"password"}'

  RÉSULTATS ATTENDUS:
  • {"status":"UP"} pour actuator/health
  • 200 OK + token pour login (si user existe)
  • 401 si credentials incorrects
  • PAS 500 !

┌──────────────────────────────────────────────────────────────────┐
│ 🔍 DIAGNOSTIC SI ÇA NE MARCHE PAS                               │
└──────────────────────────────────────────────────────────────────┘

  ERREUR: "Connection refused" sur actuator/health
  → Service pas encore démarré, attendre 30s de plus

  ERREUR: 500 sur /login après services UP
  → Problème dans User Service
  → Vérifier logs du terminal User Service
  → Chercher "Exception" ou "ERROR"

  ERREUR: 401 sur /login après services UP
  → Endpoints publics pas configurés (déjà corrigé)
  → OU credentials incorrects

  ERREUR: Neo4j connection refused
  → docker-compose up -d neo4j

┌──────────────────────────────────────────────────────────────────┐
│ 📋 CHECKLIST COMPLÈTE                                           │
└──────────────────────────────────────────────────────────────────┘

  [ ] Neo4j démarré (docker-compose up -d neo4j)
  [ ] Eureka démarré (attendre message "Started EurekaServer")
  [ ] User Service démarré (attendre "Started UserServiceApplication")
  [ ] API Gateway démarré (attendre "Netty started on port 1111")
  [ ] Movie Service démarré (attendre "Started MovieServiceApplication")

  [ ] curl localhost:1111/actuator/health → {"status":"UP"}
  [ ] curl localhost:8081/actuator/health → {"status":"UP"}

  [ ] Test login retourne 200/401 (PAS 500)
  [ ] Frontend peut se connecter

┌──────────────────────────────────────────────────────────────────┐
│ 🚨 ERREURS COURANTES ET SOLUTIONS                               │
└──────────────────────────────────────────────────────────────────┘

  1. "Port already in use"
     → kill $(lsof -t -i:1111)
     → kill $(lsof -t -i:8081)

  2. "Unable to connect to Neo4j"
     → docker-compose up -d neo4j
     → Attendre 10 secondes

  3. "Unable to register with Eureka"
     → Vérifier qu'Eureka tourne sur :8761
     → curl http://localhost:8761

  4. "BeanCreationException: SecurityConfig"
     → Dependencies Spring Security manquantes
     → Vérifier pom.xml du service

  5. Maven build échoue
     → cd <service> && mvn clean install -DskipTests

┌──────────────────────────────────────────────────────────────────┐
│ 📊 ORDRE DE DÉMARRAGE (IMPORTANT!)                              │
└──────────────────────────────────────────────────────────────────┘

  1️⃣  Neo4j         (Base de données)
      ↓ Attendre 10s

  2️⃣  Eureka        (Service discovery)
      ↓ Attendre 30s (message "Started")

  3️⃣  User Service  (Authentification)
      ↓ Attendre 20s

  4️⃣  API Gateway   (Point d'entrée)
      ↓ Attendre 20s

  5️⃣  Movie Service (Données films)
      ↓ Attendre 10s

  ✅ Tous les services UP et enregistrés sur Eureka

┌──────────────────────────────────────────────────────────────────┐
│ 🎯 APRÈS LE DÉMARRAGE                                           │
└──────────────────────────────────────────────────────────────────┘

  Frontend (déjà lancé):
  http://localhost:4200

  Actions:
  1. Va sur /login
  2. Entre email + password
  3. Clique Login
  4. Ouvre Console (F12)
  5. Vérifie les logs:
     [AuthService] Token stored: ...
     [JWT Interceptor] Token: ...

  Si login fonctionne mais /browse retourne 401:
  → Les logs te diront si le token est ajouté aux requêtes

┌──────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION                                                 │
└──────────────────────────────────────────────────────────────────┘

  Guides créés:
  • FIX_AUTHENTIFICATION_FINAL.md (ce fichier)
  • docs/FIX_500_LOGIN.md
  • docs/FIX_401_AUTH_LOGIN.md
  • docs/FIX_401_MOVIES_AFTER_LOGIN.md
  • docs/SECURITY_REFACTORING.md

  Scripts:
  • start-all.sh (terminaux multiples)
  • start-services-bg.sh (arrière-plan)
  • docker-compose.yml (Docker)

┌──────────────────────────────────────────────────────────────────┐
│ ✅ RÉSUMÉ DE TOUTE LA SESSION                                   │
└──────────────────────────────────────────────────────────────────┘

  PROBLÈMES RÉSOLUS:
  ✓ Spring Security dans tous les services → Nettoyé
  ✓ API Gateway ne démarre plus → Simplifié
  ✓ 401 sur /auth/login → Endpoints publics ajoutés
  ✓ Frontend avec logs JWT → Debug activé

  PROBLÈME ACTUEL:
  ⏳ Services backend pas démarrés → À faire maintenant

  PROCHAINES ÉTAPES:
  1. Démarre les services (Option 1, 2 ou 3 ci-dessus)
  2. Attendre 1-2 minutes
  3. Vérifier avec curl actuator/health
  4. Tester le login
  5. Rapporter les résultats

╔══════════════════════════════════════════════════════════════════╗
║              RECOMMANDATION: OPTION 1 (Docker)                   ║
╚══════════════════════════════════════════════════════════════════╝

  C'est le PLUS SIMPLE et le PLUS FIABLE:

  cd /home/pro12x/Desktop/Projects/01Dakar/Java/neo4flix
  docker-compose up -d

  Attendre 2 minutes puis:
  curl http://localhost:1111/actuator/health

  Si {"status":"UP"}:
  → Va sur http://localhost:4200/login et teste !

╔══════════════════════════════════════════════════════════════════╗
║                 LANCE LES SERVICES MAINTENANT ! 🚀               ║
╚══════════════════════════════════════════════════════════════════╝
