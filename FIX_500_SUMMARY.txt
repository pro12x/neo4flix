╔══════════════════════════════════════════════════════════════════╗
║              FIX ERREUR 500 SUR /auth/login                      ║
╚══════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────┐
│ ⚠️  PROBLÈME                                                     │
└──────────────────────────────────────────────────────────────────┘

  POST http://localhost:1111/api/v1/auth/login
  → HTTP/1.1 500 Internal Server Error ❌

  SIGNIFICATION:
  • 500 = Erreur serveur backend
  • PAS un problème de JWT ou d'endpoint public
  • Les services backend ne sont PAS démarrés
    OU ont crashé

┌──────────────────────────────────────────────────────────────────┐
│ ✅ SOLUTION IMMÉDIATE (1 commande)                              │
└──────────────────────────────────────────────────────────────────┘

  cd /home/pro12x/Desktop/Projects/01Dakar/Java/neo4flix
  ./start-backend-complete.sh

  CE SCRIPT VA:
  ✓ Rebuild API Gateway + services modifiés
  ✓ Démarrer tous les services backend
  ✓ Vérifier leur santé
  ✓ Tester le login
  ✓ Créer des logs dans logs/*.log

  TEMPS: ~2 minutes

┌──────────────────────────────────────────────────────────────────┐
│ 📋 VÉRIFICATION MANUELLE (optionnel)                            │
└──────────────────────────────────────────────────────────────────┘

  # Vérifier que les services tournent
  curl http://localhost:1111/actuator/health  # API Gateway
  curl http://localhost:8081/actuator/health  # User Service

  Si rien ne répond → Services PAS démarrés !

┌──────────────────────────────────────────────────────────────────┐
│ 🚀 ORDRE DE DÉMARRAGE (si script échoue)                        │
└──────────────────────────────────────────────────────────────────┘

  1. Neo4j:
     docker-compose up -d neo4j
     → http://localhost:7474

  2. Eureka Server:
     cd eureka-server
     mvn spring-boot:run &
     → http://localhost:8761

  3. User Service:
     cd user-service
     mvn spring-boot:run &
     → http://localhost:8081

  4. API Gateway:
     cd api-gateway
     mvn spring-boot:run &
     → http://localhost:1111

  5. Movie/Rating/Recommendation Services (optionnel)

┌──────────────────────────────────────────────────────────────────┐
│ 🧪 TEST APRÈS DÉMARRAGE                                         │
└──────────────────────────────────────────────────────────────────┘

  # 1. Gateway UP?
  curl http://localhost:1111/actuator/health
  → {"status":"UP"}

  # 2. User Service UP?
  curl http://localhost:8081/actuator/health
  → {"status":"UP"}

  # 3. Login fonctionne?
  curl -X POST http://localhost:1111/api/v1/auth/login \
    -H "Content-Type: application/json" \
    -d '{"email":"test@example.com","password":"password"}'

  RÉSULTATS ATTENDUS:
  • 200 OK + token (si user existe)
  • 401 Unauthorized (si mauvais credentials)
  • PAS 500 !

┌──────────────────────────────────────────────────────────────────┐
│ 🐛 SI LE PROBLÈME PERSISTE                                      │
└──────────────────────────────────────────────────────────────────┘

  # Vérifier les logs du User Service
  tail -f logs/User\ Service.log

  ERREURS POSSIBLES:
  • "Connection refused" → Neo4j pas démarré
  • "BeanCreationException" → Config Security cassée
  • "Authentication failed" → Problème credentials
  • "JwtTokenProvider error" → Secret JWT incorrect

┌──────────────────────────────────────────────────────────────────┐
│ 📁 FICHIERS IMPORTANTS                                          │
└──────────────────────────────────────────────────────────────────┘

  CONFIG:
  • user-service/src/main/resources/application.yml
  • api-gateway/src/main/resources/application.yml

  DEPENDENCIES:
  • user-service/pom.xml (DOIT avoir Spring Security + JWT)
  • api-gateway/pom.xml (DOIT avoir Spring Security + JWT)

  LOGS:
  • logs/User\ Service.log
  • logs/API\ Gateway.log

┌──────────────────────────────────────────────────────────────────┐
│ ✅ CHECKLIST                                                     │
└──────────────────────────────────────────────────────────────────┘

  [ ] Neo4j démarré (docker-compose up -d neo4j)
  [ ] Script lancé (./start-backend-complete.sh)
  [ ] Gateway UP (curl localhost:1111/actuator/health)
  [ ] User Service UP (curl localhost:8081/actuator/health)
  [ ] Login retourne 200/401 (PAS 500)
  [ ] Frontend peut se connecter

┌──────────────────────────────────────────────────────────────────┐
│ 📚 DOCUMENTATION                                                 │
└──────────────────────────────────────────────────────────────────┘

  Guide complet: docs/FIX_500_LOGIN.md

  Autres docs utiles:
  • docs/SECURITY_REFACTORING.md
  • docs/FIX_API_GATEWAY_SECURITY.md
  • docs/FIX_401_AUTH_LOGIN.md

┌──────────────────────────────────────────────────────────────────┐
│ 🎯 ACTION IMMÉDIATE                                             │
└──────────────────────────────────────────────────────────────────┘

  cd /home/pro12x/Desktop/Projects/01Dakar/Java/neo4flix

  # Option 1: Script automatique (RECOMMANDÉ)
  ./start-backend-complete.sh

  # Option 2: Docker
  docker-compose up -d

  # Puis teste:
  curl http://localhost:1111/actuator/health

╔══════════════════════════════════════════════════════════════════╗
║           LANCE LE SCRIPT MAINTENANT ! 🚀                        ║
╚══════════════════════════════════════════════════════════════════╝

cd /home/pro12x/Desktop/Projects/01Dakar/Java/neo4flix
./start-backend-complete.sh

Le script va:
• Tout rebuilder automatiquement
• Démarrer tous les services
• Tester le login
• Te montrer exactement ce qui ne va pas

TEMPS: 2 minutes ⏱️
