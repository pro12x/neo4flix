package com.codinggoline.recommendationservice.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // Intentionally empty: CORS is handled at the API Gateway.
}
