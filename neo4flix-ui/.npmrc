# Disable progress bars and interactive prompts in CI/Docker
progress=false
loglevel=error

# Use production dependencies only
production=true

# Disable optional dependencies
optional=false

# Disable audit during install
audit=false

# Disable fund messages
fund=false
